<!DOCTYPE html>
<html lang="ru">
<head>
  <%- include('../partials/head') %>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <%- include('../partials/header') %>

  <main class="flex-1 max-w-3xl mx-auto px-4 py-8 w-full">
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-2xl font-bold text-gray-800"><%= title %></h2>
      <a href="/warehouse/cells/create"
         class="bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium py-2 px-4 rounded-lg transition-colors">
        + Новая ячейка
      </a>
    </div>

    <div class="bg-white shadow rounded-lg overflow-hidden">
      <table class="w-full text-sm">
        <thead class="bg-gray-50 border-b">
          <tr>
            <th class="text-left px-4 py-3 font-medium text-gray-600">Название</th>
            <th class="text-left px-4 py-3 font-medium text-gray-600">Товаров</th>
            <th class="text-right px-4 py-3 font-medium text-gray-600">Действия</th>
          </tr>
        </thead>
        <tbody>
          <% cells.forEach(function(cell) { %>
            <tr class="border-b">
              <td class="px-4 py-2 text-gray-900 font-medium"><%= cell.name %></td>
              <td class="px-4 py-2 text-gray-500"><%= cell._count?.products || 0 %></td>
              <td class="px-4 py-2 text-right">
                <a href="/warehouse/cells/<%= cell.id %>/edit"
                   class="text-blue-600 hover:text-blue-800 text-xs mr-3">Редактировать</a>
                <form action="/warehouse/cells/<%= cell.id %>/delete" method="POST" class="inline m-0"
                      onsubmit="return confirm('Удалить ячейку «<%= cell.name %>»?')">
                  <button type="submit" class="text-red-500 hover:text-red-700 text-xs">Удалить</button>
                </form>
              </td>
            </tr>
          <% }); %>
          <% if (cells.length === 0) { %>
            <tr>
              <td colspan="3" class="px-4 py-8 text-center text-gray-400">Ячеек пока нет</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>

  <%- include('../partials/footer') %>

</body>
</html>
