<!DOCTYPE html>
<html lang="ru">
<head>
  <%- include('../partials/head') %>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <%- include('../partials/header') %>

  <main class="flex-1 max-w-md mx-auto px-4 py-12 w-full">
    <a href="/warehouse/users" class="text-blue-600 hover:text-blue-800 text-sm mb-4 inline-block">← Сотрудники</a>
    <h2 class="text-2xl font-bold text-gray-800 mb-6"><%= title %></h2>

    <form action="/warehouse/users/<%= employee.id %>" method="POST" class="bg-white shadow rounded-lg p-6 space-y-4">
      <div>
        <label for="userName" class="block text-sm font-medium text-gray-700 mb-1">Имя пользователя</label>
        <input type="text" id="userName" name="userName" required
          value="<%= employee.userName %>"
          minlength="3" maxlength="30"
          class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none"
        />
      </div>
      <div>
        <label for="displayName" class="block text-sm font-medium text-gray-700 mb-1">Отображаемое имя</label>
        <input type="text" id="displayName" name="displayName"
          value="<%= employee.displayName || '' %>"
          maxlength="100"
          class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none"
        />
      </div>
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
        <input type="email" id="email" name="email" required
          value="<%= employee.email %>"
          class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none"
        />
      </div>
      <div>
        <label for="role" class="block text-sm font-medium text-gray-700 mb-1">Роль</label>
        <select id="role" name="role"
          class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none">
          <option value="ADMIN" <%= employee.role === 'ADMIN' ? 'selected' : '' %>>Администратор</option>
          <option value="MANAGER" <%= employee.role === 'MANAGER' ? 'selected' : '' %>>Менеджер</option>
          <option value="EMPLOYEE" <%= employee.role === 'EMPLOYEE' ? 'selected' : '' %>>Сотрудник</option>
        </select>
      </div>
      <button type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded-lg transition-colors">
        Сохранить
      </button>
    </form>
  </main>

  <%- include('../partials/footer') %>

</body>
</html>
